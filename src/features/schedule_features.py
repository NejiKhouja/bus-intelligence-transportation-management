"""Placeholder for schedule-related features (headway, layover, shift
coverage). Not started: no dedicated schedule/timetable collection was
found during discovery — see docs/database.md, section "Schedules /
Trips". The only schedule-like data is the sparse `horaires` array
embedded in some winicari.station documents, which is insufficient on
its own (see docs/data_dictionary.md).
"""
