pymongo>=4.17
python-dotenv>=1.0
pandas>=2.2
numpy>=1.26
pyarrow>=16.0

# Modeling — HistGradientBoostingRegressor (sklearn) used instead of
# LightGBM/XGBoost for now to avoid adding dependencies beyond what's
# already installed; swap in if sklearn's accuracy proves insufficient.
scikit-learn>=1.5

# Optimization
ortools>=9.10

# Notebooks
jupyter>=1.0

# Testing
pytest>=8.0
